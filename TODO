Carousel:
[ ] Node insertion does not set focus properly; focus is lost

Lisp demo:
[ ] Changing graph doesn't update text

EBNF demo:
[ ] PrimaryDecoration has only a choice as value. It should still be nested.
[ ] A SequenceOrDifference value appears nested when it doesn't need to be.
[ ] DecorationName is irrelevant
[ ] Url and Link are irrelevant
